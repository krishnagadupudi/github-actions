name: 02-workflow events

on: workflow_dispatch



jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
      - name: show the trigger
        run: echo "The workflow was triggered by the ${{ github.event_name }} event."
